extends layout.pug
    
block content
    h1 Welcome, #{user.id}!
    img(src = user.images[0].url, class = 'circular_image')

    h2 Your last 50 saved tracks
    h4 Get recommendations from your new faves! 🔎
    <p>
    table
      each t in tracks 
        tr 
        - var artist_name = t.track.artists[0].name 
        - var track_name = t.track.name
        - var track_preview = t.track.preview_url
        - var artist_id = t.track.artists
        - var track_id = t.track.id
        
        td #{artist_name}
        td #{track_name}
        td: a(href=track_preview) Preview&nbsp;
        td: a(href='/recommendations?&track=' + track_id) Recommend similar songs

    h3 ☞ Should be a qualtrics thing here!   
    iframe(src="https://york.qualtrics.com/jfe/form/SV_b49SRHLYp8u1OOW", height="800px", width="1250px")
